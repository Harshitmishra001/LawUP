"""Ingestion Agent — parses uploaded PDF/DOCX, extracts clause-level text units."""


class IngestionAgent:
    """Ingestion Agent — parses uploaded PDF/DOCX, extracts clause-level text units.

    Pipeline step: 1 (reference Section 5 of PLAN.md)
    """

    def __init__(self):
        pass

    def run(self, *args, **kwargs):
        raise NotImplementedError("Implementation pending — see M4 milestone.")
